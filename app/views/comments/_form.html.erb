<%= form_for([@post,@post.comments.build]) do |f| %>
	<div class="field">
		<%= f.label:commenter %><br/>
		<%= f.text_field:commter %>
	</div>
	<div class="field">
		<%= f.label:description %><br/>
		<%= f.text_area:body %>
	</div>
	<div class="field">
		<%= f.submit %>
	</div>
<% end %>